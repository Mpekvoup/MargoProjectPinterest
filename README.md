# Pinterest Clone

Полнофункциональное веб-приложение в стиле Pinterest с возможностью сохранения изображений, созданием досок и системой аутентификации.

## ✨ Особенности

- 📌 **Пины**: Загружайте и сохраняйте изображения
- 📋 **Доски**: Создавайте и организуйте коллекции пинов
- 🎨 **Masonry Layout**: Красивый адаптивный макет в стиле Pinterest
- 👤 **Аутентификация**: Регистрация и вход пользователей
- 🔍 **Поиск**: Находите пины по названию, описанию и тегам
- ❤️ **Лайки**: Ставьте лайки понравившимся пинам
- 💬 **Комментарии**: Обсуждайте пины с другими пользователями
- 📱 **Адаптивный дизайн**: Работает на всех устройствах

## 🛠 Технологический стек

### Backend
- **Django 5.0** - Python веб-фреймворк
- **SQLite** - База данных (можно заменить на PostgreSQL)
- **Pillow** - Обработка изображений
- **Django Templates** - Шаблонизатор

### Frontend
- **HTML5** - Структура
- **CSS3** - Стилизация с CSS Grid и Flexbox
- **JavaScript (Vanilla)** - Интерактивность без фреймворков

## 📁 Структура проекта

```
MargoProjectPinterest/
├── backend/
│   ├── pinterest_project/      # Основной проект Django
│   │   ├── settings.py
│   │   ├── urls.py
│   │   └── wsgi.py
│   ├── pins/                   # Приложение для пинов и досок
│   │   ├── models.py          # Модели (Pin, Board, Comment)
│   │   ├── views.py           # Представления
│   │   ├── forms.py           # Формы
│   │   ├── urls.py            # URL-маршруты
│   │   └── admin.py           # Админ-панель
│   ├── templates/             # HTML шаблоны
│   │   ├── base.html
│   │   ├── pins/
│   │   └── registration/
│   ├── static/                # Статические файлы
│   │   ├── css/
│   │   ├── js/
│   │   └── images/
│   ├── media/                 # Загруженные изображения
│   ├── manage.py
│   └── requirements.txt
├── .gitignore
├── package.json
└── README.md
```

## 🚀 Быстрый старт

### Предварительные требования

- Python 3.10 или выше
- pip (менеджер пакетов Python)
- Виртуальное окружение (рекомендуется)

### Установка

1. **Клонируйте репозиторий**
   ```bash
   git clone <repository-url>
   cd MargoProjectPinterest
   ```

2. **Создайте виртуальное окружение**
   ```bash
   cd backend
   python -m venv venv
   ```

3. **Активируйте виртуальное окружение**

   **Windows:**
   ```bash
   venv\Scripts\activate
   ```

   **Linux/Mac:**
   ```bash
   source venv/bin/activate
   ```

4. **Установите зависимости**
   ```bash
   pip install -r requirements.txt
   ```

5. **Создайте файл .env** (опционально)
   ```bash
   cp .env.example .env
   ```

   Отредактируйте `.env` и установите свои значения:
   ```env
   SECRET_KEY=your-secret-key-here
   DEBUG=True
   ALLOWED_HOSTS=localhost,127.0.0.1
   ```

6. **Выполните миграции базы данных**
   ```bash
   python manage.py makemigrations
   python manage.py migrate
   ```

7. **Создайте суперпользователя** (для доступа к админ-панели)
   ```bash
   python manage.py createsuperuser
   ```

8. **Соберите статические файлы** (для продакшна)
   ```bash
   python manage.py collectstatic --noinput
   ```

9. **Запустите сервер разработки**
   ```bash
   python manage.py runserver
   ```

10. **Откройте в браузере**

    Перейдите по адресу: `http://127.0.0.1:8000`

    Админ-панель: `http://127.0.0.1:8000/admin`

## 📋 Основные URL-маршруты

### Главные страницы
- `/` - Главная страница с лентой пинов
- `/boards/` - Список всех публичных досок
- `/user/<username>/` - Профиль пользователя

### Пины
- `/pin/<id>/` - Детальная страница пина
- `/pin/create/` - Создать новый пин
- `/pin/<id>/edit/` - Редактировать пин
- `/pin/<id>/delete/` - Удалить пин
- `/pin/<id>/like/` - Поставить/убрать лайк (AJAX)

### Доски
- `/board/<slug>/` - Детальная страница доски
- `/board/create/` - Создать доску
- `/board/<slug>/edit/` - Редактировать доску
- `/board/<slug>/delete/` - Удалить доску

### Аутентификация
- `/register/` - Регистрация
- `/login/` - Вход
- `/logout/` - Выход

## 🎨 Особенности интерфейса

### Masonry Layout
Адаптивная сетка с использованием CSS `column-count`, автоматически подстраивается под размер экрана:
- 5 колонок на больших экранах (> 1400px)
- 4 колонки на десктопах (> 1024px)
- 3 колонки на планшетах (> 768px)
- 2 колонки на мобильных (> 480px)
- 1 колонка на маленьких экранах

### Интерактивность
- ❤️ AJAX лайки без перезагрузки страницы
- 🖼️ Предпросмотр изображений перед загрузкой
- 📱 Адаптивная навигация
- ⌨️ Горячие клавиши (Ctrl+K для поиска)
- 🔝 Кнопка "Наверх" при прокрутке

## 🗄️ Модели данных

### Pin (Пин)
- `title` - Название
- `description` - Описание
- `image` - Изображение
- `source_url` - Ссылка на источник
- `user` - Автор
- `board` - Доска
- `tags` - Теги (через запятую)
- `likes` - Лайки (ManyToMany)

### Board (Доска)
- `name` - Название
- `description` - Описание
- `user` - Владелец
- `is_private` - Приватная доска
- `slug` - URL-slug

### Comment (Комментарий)
- `pin` - Пин
- `user` - Автор
- `text` - Текст комментария

## 🔧 Разработка

### Создание нового приложения Django
```bash
python manage.py startapp app_name
```

### Работа с миграциями
```bash
python manage.py makemigrations    # Создать миграции
python manage.py migrate           # Применить миграции
python manage.py showmigrations    # Показать статус миграций
```

### Запуск Django Shell
```bash
python manage.py shell
```

### Работа со статическими файлами
```bash
python manage.py collectstatic     # Собрать статику
python manage.py findstatic style.css  # Найти файл в статике
```

### Создание дампа данных
```bash
python manage.py dumpdata > data.json
python manage.py loaddata data.json
```

## 🚀 Деплой в продакшн

### Настройка для продакшна

1. Установите `DEBUG=False` в settings.py
2. Настройте `ALLOWED_HOSTS`
3. Используйте PostgreSQL вместо SQLite
4. Настройте nginx для отдачи статики и медиа
5. Используйте gunicorn для запуска Django
6. Настройте HTTPS сертификат

### Запуск с Gunicorn
```bash
pip install gunicorn
gunicorn pinterest_project.wsgi:application --bind 0.0.0.0:8000
```

### Использование Docker
```bash
# Создать образ
docker build -t pinterest-clone .

# Запустить контейнер
docker run -p 8000:8000 pinterest-clone
```

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции (`git checkout -b feature/AmazingFeature`)
3. Закоммитьте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Запушьте в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📝 TODO / Планы развития

- [ ] Добавить пагинацию / бесконечную прокрутку
- [ ] Реализовать drag-and-drop загрузку изображений
- [ ] Добавить уведомления в реальном времени
- [ ] Интеграция с внешними API (Unsplash, Pexels)
- [ ] Подписки на пользователей
- [ ] Приватные сообщения
- [ ] Экспорт досок в PDF
- [ ] Мобильное приложение
- [ ] REST API с Django REST Framework

## 📄 Лицензия

Этот проект лицензирован под MIT License.

## 🙏 Благодарности

- Вдохновлено [Pinterest](https://www.pinterest.com)
- Создано для обучения и демонстрации возможностей Django
- Использует современные веб-технологии

## 📞 Контакты

Если у вас есть вопросы или предложения, создайте Issue в репозитории.

---

**Приятного использования! 🎨📌**
